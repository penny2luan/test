package sorts;

import java.util.ArrayList;
import java.util.Comparator;

public class MergeSort
{
	public static <T> void sort(T[] x, Comparator<T> c)
	{
		
	}
	
	private static <T> void sort(T[] x, Comparator<T> c, int low, int high)
	{
		
	}
	
	private static <T> void merge(T[] x, Comparator<T> c, int low, int mid, int high)
	{
		
	}
}